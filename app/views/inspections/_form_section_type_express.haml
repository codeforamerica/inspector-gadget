.row
  .margin-bottom-10 What is the inspection type?
  .field
    = f.hidden_field :inspection_type_id
  .max-width-px-400
    %fieldset
      - InspectionType.pluck(:inspection_supercategory).uniq.map{|type| [type.titleize, type] }.each do |sc|
        %input{ type:'radio', class:"inspection-supercategories", name:"inspection-supercategories", value:sc[1], id:sc[1] }
        %label{ for:sc[1] } #{sc[0]}
  .max-width-px-400
    %select.inspection-names{ style: "display:none;" }
      -# options are inserted here by JS
  .max-width-px-400
    .inspection-type-comments.margin-bottom-10
